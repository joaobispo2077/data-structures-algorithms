class Node {
  constructor(data) {
    this.data = data;
    this.next = null;
  }
}

// const node = new Node(2);

// console.log(node);

// const node2 = new Node(12);
// node.next = node2;

// console.log(node);
// module.exports = { Node }
module.exports = { Node }